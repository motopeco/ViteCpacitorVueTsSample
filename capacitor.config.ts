import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'jp.pelife.shopgame',
  appName: 'shop',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
